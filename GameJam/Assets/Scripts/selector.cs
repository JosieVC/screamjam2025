using UnityEngine;

public class selector : MonoBehaviour
{
    [SerializeField]
    GameObject blueprint;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void PickBlueprint()
    {
        //allow players to select between owned blueprints (remember new blueprints are unlocked each day)
        //then replace the blueprint sprite with the chosen blueprint
    }
}
