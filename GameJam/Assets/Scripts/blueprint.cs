using UnityEngine;

public class bluepriint : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void MakeOrgan()
    {
        //this is the bulk of this scene - check to see if the player has enough parts for the recipe, if so, place them on the blueprint,
        //assemble the object and award the player money (money is a static int, access it with assemblyManager.money)
    }
}
